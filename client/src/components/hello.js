import React from "react";
import { useEffect, useState } from "react";

/*
export const Hello = () => {
  const [initialState, setInititalState] = useState([]);

  useEffect(() => {
    fetch("/api/")
      .then((res) => {
        if (res.ok) {
          return res.json();
        }
      })
      .then((jsonResponse) => setInititalState(jsonResponse.Hello));
  }, []);

  console.log(initialState);

  return (
    <div>
      {initialState.map((e) => {
        <div>{e}</div>;
      })}
      <h1> Hello</h1>
    </div>
  );
};
*/
